node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Sensitive VAPID keys — use VAPID_PUBLIC_KEY / VAPID_PRIVATE_KEY env vars in production
data/vapid-keys.json
